<div class="row">
  <div class="col-md-6" id="lot-header">
    <h1>Lot ID: {{id}}</h1>
    <h3><strong>Starting Bid: </strong> ${{startingBid}}.00</h3>
    <h3><strong>Description:</strong> {{description}}</h3>
  </div>
  <div class="col-md-6">
    <img class="img-responsive img-rounded" id="display-item" {{bind-attr src=this.image}}>
  </div>
</div>

  <div class="row">
    {{#if this.notViewingItems}}
      <div class="col-md-8" {{action 'viewItems'}}>
        {{#link-to 'items' id classNames="btn btn-primary"}}Click Here To View Items{{/link-to}}
      </div>
    {{/if}}
  </div>


{{outlet "items"}}

<br>
{{outlet "new-item"}}

{{#if this.notAddingItem}}
  <div class="row">
    <div class="col-md-8" {{action 'addItem'}}>
      {{#link-to 'new-item' classNames="btn btn-primary"}} Add item {{/link-to}}
    </div>
  </div>
{{/if}}

<!-- this.items | itemController='item' -->
<!-- item in Auction.ItemsController -->
