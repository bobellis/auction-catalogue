<h1>{{name}}</h1>
<h3>Auction Date: {{date}}</h3>
<h4>{{description}}</h4>

<ul>
  {{#each lot in lots}}
  <li>{{#link-to 'lot' lot.id}}{{lot.description}}<p class="lot-id"> Lot id: {{lot.id}} </p>{{/link-to}}</li>
  {{/each}}
</ul>

{{#link-to 'new-lot' classNames="btn btn-primary"}} New Lot {{/link-to}}
